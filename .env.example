# Copy to .env and fill in with your MongoDB connection string (MongoDB Atlas recommended)
# Example (replace <user>, <pass>, <cluster> and <dbname>):
# DATABASE_URL="mongodb+srv://<user>:<pass>@<cluster>.mongodb.net/<dbname>?retryWrites=true&w=majority"

DATABASE_URL="mongodb+srv://<user>:<pass>@cluster0.mongodb.net/StorefrontDB?retryWrites=true&w=majority"

# Vercel Blob token (used for Vercel Blob / object storage). Do NOT store real tokens in the repo.
# For local development copy this to your `.env`, and in production set the variable in your host (Vercel, Heroku, etc.).
VERCEL_BLOB_TOKEN="your_vercel_blob_token_here"
