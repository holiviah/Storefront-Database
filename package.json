{
  "name": "storefront-database",
  "version": "1.0.0",
  "description": "ReVamp Storefront",
  "main": "server.js",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:db:push": "prisma db push",
    "check": "node src/index.js",
    "start": "node server.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "dependencies": {
    "@prisma/client": "^5.0.0",
    "express": "^4.18.2",
    "multer": "^2.0.2",
    "morgan": "^1.10.0",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "prisma": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0 <23.0.0"
  }
}
